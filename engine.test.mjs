import assert from'node:assert/strict';import{Game,evaluate,beats,choose}from'./public/engine.mjs';
assert.equal(evaluate([0,1]).name,'Para');assert.equal(evaluate([0,0]),null);assert.equal(evaluate([0,4]),null);assert.equal(evaluate([0,4,8,12,16]).cat,5);assert.equal(evaluate([0,5,10,15,16]).cat,1);assert.equal(evaluate([0,1,2,4,5]).cat,3);assert.equal(evaluate([0,1,2,3,5]).cat,4);assert(!beats(evaluate([0,1]),evaluate([2])));assert(beats(evaluate([3]),evaluate([2])));
let moves=0;for(const count of[3,4]){for(let match=0;match<30;match++){const g=new Game(count);for(let r=1;r<=10;r++){assert.equal(g.round,r);assert.equal(g.players.flatMap(p=>p.hand).length,47);assert.equal(g.hidden.length,4);const all=[...g.hidden,...g.players.flatMap(p=>p.hand),...g.current.cards];assert.equal(new Set(all).size,52);const before=g.players.map(p=>p.score);let turn=0;while(!g.over&&turn++<1000){const actor=g.turn,c=choose(g.players[actor].hand,g.current);if(c)g.play(actor,c.cards);else g.pass(actor);moves++;}assert(g.over);assert.equal(g.players[g.winner].hand.length,0);g.players.forEach((p,i)=>assert.equal(p.score,before[i]+p.hand.length));if(r<10)g.nextRound();}}}
console.log(`PASS: rules, deal uniqueness, scoring and 60 complete matches (600 rounds, ${moves} legal moves).`);
